@*No of Pizza Available = @Model.Count
<div class="table-responsive"></div>
<p>Details</p>
<table class="table">

    @foreach (Pizza p in Model)
    {
        <tr><td>@p.Name</td></tr>
        
    }
</table>
</div>*@

<partial name="_PizzaList" />
